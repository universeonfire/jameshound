<%= form_with(model: cari_grup, local: true) do |form| %>
  <% if cari_grup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cari_grup.errors.count, "error") %> prohibited this cari_grup from being saved:</h2>

      <ul>
      <% cari_grup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :grup_adi %>
    <%= form.text_field :grup_adi %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
